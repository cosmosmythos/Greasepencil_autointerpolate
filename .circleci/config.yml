version: 2.1

# Define reusable commands
commands:
  setup_python:
    steps:
      - run:
          name: Setup Python (Linux)
          command: |
            python -m pip install --upgrade pip
            pip install setuptools wheel build

jobs:
  # Build Interpolate Modules
  build_interpolate_windows:
    machine:
      image: windows-server-2022-gui:current
    resource_class: windows.medium
    steps:
      - checkout
      - run:
          name: Install dependencies (Windows)
          shell: powershell.exe
          command: |
            choco install cmake visualstudio2022buildtools -y
            # Pin Python exactly like GitHub Actions
            choco install python --version=3.11.9 -y --force
            py -3.11 --version
            & "C:\Program Files\CMake\bin\cmake.exe" --version
      - run:
          name: Install Python build deps (Windows)
          shell: powershell.exe
          command: |
            py -3.11 -m pip install --upgrade pip
            py -3.11 -m pip install setuptools wheel build
      - run:
          name: Build C++ extension (Windows)
          shell: powershell.exe
          command: |
            New-Item -ItemType Directory -Force -Path Executable\build | Out-Null
            Set-Location Executable\build
            & "C:\Program Files\CMake\bin\cmake.exe" .. -G "Visual Studio 17 2022" -A x64 -DCMAKE_BUILD_TYPE=Release
            & "C:\Program Files\CMake\bin\cmake.exe" --build . --config Release
            Set-Location ../..
      - run:
          name: Build wheel (Windows)
          shell: powershell.exe
          command: |
            py -3.11 setup.py bdist_wheel
      - store_artifacts:
          path: dist/*.whl
      - persist_to_workspace:
          root: .
          paths:
            - dist/*.whl

  build_interpolate_linux:
    docker:
      - image: cimg/python:3.11
    steps:
      - checkout
      - run:
          name: Install dependencies
          command: |
            sudo apt-get update
            sudo apt-get install -y cmake build-essential
      - setup_python
      - run:
          name: Build C++ extension
          command: |
            mkdir -p Executable/build
            cd Executable/build
            cmake .. -DCMAKE_BUILD_TYPE=Release
            cmake --build . --config Release
            cd ../..
      - run:
          name: Build wheel
          command: |
            python setup.py bdist_wheel
      - store_artifacts:
          path: dist/*.whl
      - persist_to_workspace:
          root: .
          paths:
            - dist/*.whl

  # Build LineVector Module
  build_linevector_windows:
    machine:
      image: windows-server-2022-gui:current
    resource_class: windows.medium
    steps:
      - checkout
      - run:
          name: Install dependencies (Windows)
          shell: powershell.exe
          command: |
            choco install cmake visualstudio2022buildtools -y
            # Pin Python exactly like GitHub Actions
            choco install python --version=3.11.9 -y --force
            # Match GitHub Actions: opencv + eigen + boost-msvc-14.3
            choco install opencv eigen boost-msvc-14.3 -y
            py -3.11 --version
            & "C:\Program Files\CMake\bin\cmake.exe" --version
      - run:
          name: Install Python build deps (Windows)
          shell: powershell.exe
          command: |
            py -3.11 -m pip install --upgrade pip
            py -3.11 -m pip install setuptools wheel build
      - run:
          name: Ensure simple_svg header exists
          shell: powershell.exe
          command: |
            $outFile = "Vectorize/src_polyvector/simple_svg_1.0.0.hpp"
            if (Test-Path $outFile) {
              Write-Host "Header already present: $outFile"
            } else {
              Write-Host "Downloading missing header to: $outFile"
              Invoke-WebRequest -Uri "https://raw.githubusercontent.com/adishavit/simple-svg/master/simple_svg_1.0.0.hpp" -OutFile $outFile
            }
      - run:
          name: Configure & build C++ extension (Windows)
          shell: powershell.exe
          command: |
            New-Item -ItemType Directory -Force -Path Vectorize\build | Out-Null
            Set-Location Vectorize\build
            # Match GitHub Actions OpenCV_DIR
            & "C:\Program Files\CMake\bin\cmake.exe" .. -G "Visual Studio 17 2022" -A x64 -DOpenCV_DIR="C:/tools/opencv/build"
            & "C:\Program Files\CMake\bin\cmake.exe" --build . --config Release
            Set-Location ../..
      - run:
          name: Build wheel (Windows)
          shell: powershell.exe
          command: |
            py -3.11 setup_vectorize.py bdist_wheel
      - run:
          name: Bundle dependencies (Windows)
          shell: powershell.exe
          command: |
            py -3.11 -m pip install delvewheel
            Set-Location dist
            Get-ChildItem -Filter *.whl | ForEach-Object {
              Write-Host "Repairing wheel: $($_.Name)"
              py -3.11 -m delvewheel repair $_.Name -w . --add-path "C:/tools/opencv/build/x64/vc16/bin"
            }
            Set-Location ..
      - store_artifacts:
          path: dist/*.whl
      - persist_to_workspace:
          root: .
          paths:
            - dist/*.whl

  build_linevector_linux:
    docker:
      - image: cimg/python:3.11
    steps:
      - checkout
      - run:
          name: Install dependencies
          command: |
            sudo apt-get update
            sudo apt-get install -y build-essential cmake libopencv-dev libeigen3-dev libboost-dev curl patchelf
      - setup_python
      - run:
          name: Download missing headers
          command: |
            curl -fsSL -o Vectorize/src_polyvector/simple_svg_1.0.0.hpp https://raw.githubusercontent.com/adishavit/simple-svg/master/simple_svg_1.0.0.hpp
      - run:
          name: Build C++ extension
          command: |
            mkdir -p Vectorize/build
            cd Vectorize/build
            cmake .. -DCMAKE_BUILD_TYPE=Release
            cmake --build . --config Release
            cd ../..
      - run:
          name: Build wheel
          command: |
            python setup_vectorize.py bdist_wheel
      - run:
          name: Bundle dependencies
          command: |
            pip install auditwheel
            cd dist
            for wheel in *.whl; do
              auditwheel repair "$wheel" -w .
            done
      - store_artifacts:
          path: dist/*.whl
      - persist_to_workspace:
          root: .
          paths:
            - dist/*.whl

  # Package Everything
  package:
    docker:
      - image: cimg/base:current
    steps:
      - checkout
      - attach_workspace:
          at: .
      - run:
          name: Install zip
          command: sudo apt-get update && sudo apt-get install -y zip
      - run:
          name: Create platform packages
          command: |
            # Create directories
            mkdir -p build_windows/Addon build_linux/Addon
            
            # Copy addon files
            cp -r Addon/* build_windows/Addon/
            cp -r Addon/* build_linux/Addon/
            
            # Create wheels directories
            mkdir -p build_windows/Addon/wheels build_linux/Addon/wheels
            
            # Copy wheels (find all available wheels)
            find dist -name "*win*.whl" -exec cp {} build_windows/Addon/wheels/ \; 2>/dev/null || true
            find dist -name "*linux*.whl" -exec cp {} build_linux/Addon/wheels/ \; 2>/dev/null || true
            find dist -name "*manylinux*.whl" -exec cp {} build_linux/Addon/wheels/ \; 2>/dev/null || true
            
            # Create ZIPs
            cd build_windows && zip -r ../gp_auto_interpolate-windows.zip Addon/
            cd ../build_linux && zip -r ../gp_auto_interpolate-linux.zip Addon/
            
            # Show what we built
            cd ..
            ls -la *.zip
            echo "Windows package contents:"
            unzip -l gp_auto_interpolate-windows.zip | head -20
            echo "Linux package contents:"
            unzip -l gp_auto_interpolate-linux.zip | head -20
      - store_artifacts:
          path: "*.zip"

workflows:
  build_and_package:
    jobs:
      - build_interpolate_windows
      - build_interpolate_linux
      - build_linevector_windows
      - build_linevector_linux
      - package:
          requires:
            - build_interpolate_windows
            - build_interpolate_linux
            - build_linevector_windows
            - build_linevector_linux